{
  "name": "fullstack-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd server && npm install && cd ../client && npm install --legacy-peer-deps",
    
    "build": "cd client && npx vite build",
    
    "start": "node server/src/index.js"
  }
}
